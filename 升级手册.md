# 🔮 系统升级与商业化路线图

---

## 目录

1. [现状评估](#1-现状评估)
2. [技术债务与改进清单](#2-技术债务与改进清单)
3. [功能升级路线图](#3-功能升级路线图)
4. [商业化路径](#4-商业化路径)
5. [技术架构演进](#5-技术架构演进)

---

## 1. 现状评估

### 1.1 已完成的 MVP 能力

| 能力 | 状态 | 说明 |
|---|---|---|
| 4 Agent DAG 流水线 | ✅ | Collector → Analyst → Writer → Reviewer |
| 反馈闭环 | ✅ | 质检驳回自动回退，最多 3 轮 |
| 字段级溯源 | ✅ | 每条结论绑定 Evidence + source_id |
| 可观测性 | ✅ | TraceStore + ArtifactStore + MessageBus 日志 |
| Mock 降级模式 | ✅ | 无 API Key 也能跑通全流程 |
| 测试覆盖 | ✅ | 128 条单元+集成测试 |
| Streamlit 前端 | ✅ | Web UI 可用 |
| 多 LLM 支持 | ✅ | OpenAI / DeepSeek / Anthropic |

### 1.2 现状评分

| 维度 | 评分 | 说明 |
|---|---|---|
| 架构设计 | ⭐⭐⭐⭐ | DAG 编排 + 条件路由 + 溯源设计合理 |
| 代码质量 | ⭐⭐⭐ | 有测试但缺类型检查强制执行 |
| 可运行性 | ⭐⭐⭐⭐ | mock 模式零依赖可跑 |
| 真实可用性 | ⭐⭐ | 依赖外部 LLM + 搜索 API，prompt 未调优 |
| 安全性 | ⭐⭐ | 无鉴权、无速率限制 |
| 可扩展性 | ⭐⭐⭐ | 架构支持但未验证多租户 |
| 可维护性 | ⭐⭐⭐ | 结构清晰但 prompt 硬编码 |

---

## 2. 技术债务与改进清单

### 2.1 🔴 严重（阻塞生产部署）

#### 2.1.1 LangGraph 集成未验证

**现状**：Orchestrator 中有 LangGraph 代码路径，但实际运行始终走顺序 fallback。LangGraph 从未在集成测试中被真正调用过。

**风险**：DAG 节点定义、条件路由、recursion_limit 等代码未经实战验证。

**改进**：
```python
# 需要修复的点
# 1. AgentState 是 TypedDict，但 LangGraph 的 add_node 期望可调用函数
# 2. 节点函数签名需要与 StateGraph 的 reducer 机制对齐
# 3. 条件路由函数 _route_after_review 的返回值需要与 add_conditional_edges 的 mapping 完全匹配
```

**建议**：专门写一个 `test_langgraph_integration.py`，用 mock LLM 跑完整的 LangGraph 编译+invoke 流程。

#### 2.1.2 异常处理不完整

**现状**：
- `WebScraperTool.fetch` 有 try/except，但 Collector 调用时如果全部 URL 抓取失败，会传入空字符串给 `DataParser`
- Analyst 在无 LLM 时有降级逻辑，但 `_invoke_llm` 本身如果是网络错误（非 None LLM），没有重试机制
- Reviewer 的 `_parse_review_response` 兜底返回 `passed=True`，可能导致坏报告静默通过

**改进**：
- LLM 调用层加指数退避重试（3 次）
- 搜索 API 加熔断器
- Reviewer 在 LLM 返回不可解析时，应返回 `passed=False`（保守策略）

#### 2.1.3 消息总线是内存实现

**现状**：`MessageBus` 所有数据在内存中，进程退出即丢失。跨会话无法追溯。

**改进**：
- 接入 Redis Pub/Sub 作为消息后端
- 所有消息同时写 TraceStore 做持久化（当前 publish 时已存 message_log，但未持久化到磁盘 — 只在 `_save_traces` 时批量写）

#### 2.1.4 无输入校验

**现状**：用户可以输入空字符串、超长字符串、恶意 prompt 注入内容。

**改进**：
- 输入层加 Pydantic 校验（长度限制、字符白名单）
- LLM prompt 中加防御性 system prompt 防注入

### 2.2 🟡 中等（影响质量与效率）

#### 2.2.1 Prompt 管理混乱

**现状**：Prompt 散落在 6 个文件中（`agents.yaml`、`data_parser.py`、`feature.py`、`market.py`、`review.py`、`writer.py`），有 YAML 模板也有 Python 常量。修改一个 prompt 需要知道它在哪里定义。

**改进**：
```
prompts/
├── collector.yaml
├── analyst_comparison.yaml
├── analyst_insight.yaml
├── writer_summary.yaml
├── writer_recommendations.yaml
├── reviewer.yaml
└── parser.yaml
```
统一由 `PromptRegistry` 加载和管理，支持变量插值。

#### 2.2.2 缺少结构化日志与告警

**现状**：用 loguru 打日志，但没有结构化输出（JSON 格式），没有告警规则。

**改进**：
- 日志输出为 JSON（ELK 可消费）
- 定义关键指标告警：质检评分 < 0.5、LLM 调用超时 > 3 次、搜索 API 返回空结果

#### 2.2.3 测试中 Mock 与真实路径差距大

**现状**：单元测试用 `MagicMock` 模拟 LLM 返回完美 JSON，但真实 LLM 可能返回非 JSON、超长内容、乱码。集成测试也是 mock LLM → 128 条测试全绿不代表真实可用。

**改进**：
- 增加"对抗性测试"：模拟 LLM 返回非 JSON、空字符串、超长内容
- 增加真实 LLM 回归测试（CI 中可选运行，需配置 Key）

#### 2.2.4 网页抓取过于简陋

**现状**：`web_scraper.py` 用正则提取文本，不支持 JavaScript 渲染、不支持反爬绕过、不支持 PDF 解析。

**改进**：
- 集成 `playwright` 或 `selenium` 做 JS 渲染
- 集成 `BeautifulSoup` 做结构化 HTML 解析
- 加 `robots.txt` 检查和请求频率控制

### 2.3 🟢 轻微（不影响功能但影响体验）

#### 2.3.1 缺少进度回调

**现状**：Streamlit 前端用 `st.progress` 硬编码百分比，与实际执行不同步。

**改进**：Orchestrator 提供实时回调钩子，前端订阅进度事件。

#### 2.3.2 配置文件硬编码路径

**现状**：`config/settings.yaml` 中的路径（`./traces`、`./outputs`）是相对路径，受工作目录影响。

**改进**：支持绝对路径 + 环境变量插值（`${PROJECT_ROOT}/traces`）。

#### 2.3.3 缺少 CLI 工具

**现状**：只能通过 Python 代码或 Streamlit 使用。

**改进**：
```bash
# 目标体验
competitive-analysis run "Notion" --dimensions 功能,定价,体验 --output report.md
competitive-analysis trace list
competitive-analysis trace show <trace_id>
```

---

## 3. 功能升级路线图

### Phase 1: 工程加固（2-3 周）

| 任务 | 优先级 | 预估工时 |
|---|---|---|
| LangGraph 集成验证 + 测试 | 🔴 | 3d |
| LLM 调用重试 + 熔断 | 🔴 | 2d |
| 输入校验 + 防注入 | 🔴 | 1d |
| Prompt 统一管理 | 🟡 | 2d |
| 异常处理完善 | 🔴 | 2d |
| 对抗性测试补充 | 🟡 | 2d |
| **小计** | | **12d** |

### Phase 2: 分析能力增强（3-4 周）

| 任务 | 说明 | 预估工时 |
|---|---|---|
| **多竞品同时分析** | 当前 MVP 只支持单竞品，升级为输入多个产品名并行采集+对比 | 5d |
| **竞品历史追踪** | 定期（每周/每月）自动重分析，产出变化趋势图 | 5d |
| **情感分析** | 采集用户评论/舆情数据，分析市场口碑 | 3d |
| **定价变化监控** | 自动检测竞品定价页面变化并告警 | 3d |
| **行业报告引用** | 集成 Gartner/IDC 等行业报告作为分析上下文 | 3d |
| **小计** | | **19d** |

### Phase 3: 协作与集成（4-5 周）

| 任务 | 说明 | 预估工时 |
|---|---|---|
| **报告导出** | PDF（WeasyPrint）+ PPT（python-pptx）+ HTML | 5d |
| **Slack/飞书通知** | 分析完成后自动推送摘要到 IM | 2d |
| **Notion/Confluence 发布** | 报告一键发布到知识库 | 3d |
| **分析模板市场** | 预设分析模板（SaaS 竞品、电商竞品、AI 产品等） | 5d |
| **多人协作** | 分析任务共享、评论、标注 | 10d |
| **小计** | | **25d** |

### Phase 4: 智能增强（6-8 周）

| 任务 | 说明 | 预估工时 |
|---|---|---|
| **自适应分析深度** | 根据竞品类型自动调整分析维度和 depth | 5d |
| **竞品关系图谱** | Neo4j 构建竞品间的投资/技术/人才关系图 | 10d |
| **预测性分析** | 基于历史数据预测竞品下一步动作（融资/新品/定价调整） | 10d |
| **多语言支持** | 支持英文/日文/韩文竞品分析，自动翻译报告 | 5d |
| **自定义 Agent** | 用户可拖拽编排自己的分析流水线 | 15d |
| **小计** | | **45d** |

---

## 4. 商业化路径

### 4.1 产品定位

```
┌──────────────────────────────────────────────────┐
│              竞品分析 Agent 协作系统                │
│                                                    │
│  从"单人手工 Google → Excel"升级为                   │
│  "AI Agent 自动采集 → 分析 → 报告 → 持续监控"       │
└──────────────────────────────────────────────────┘
```

**目标用户画像**：
- 产品经理（需要快速了解竞品动态）
- 战略/投资分析师（需要深度行业洞察）
- 市场/营销团队（需要竞品定位对比）
- 创业者（需要验证赛道竞争格局）

### 4.2 商业化里程碑

#### Milestone 1: SaaS MVP（3 个月）

| 能力 | 说明 |
|---|---|
| 用户注册/登录 | OAuth（Google/GitHub）+ 邮箱 |
| 配额管理 | 免费 3 次/月，付费无限 |
| 报告管理 | 历史报告列表、搜索、删除 |
| 支付集成 | Stripe / 支付宝 |
| 基础监控 | 每月自动分析 + 邮件推送 |

**定价策略**：
| 套餐 | 价格 | 内容 |
|---|---|---|
| Free | $0/月 | 3 次分析/月，基础维度 |
| Pro | $29/月 | 无限分析，全维度，PDF 导出 |
| Team | $99/月 | 5 人协作，竞品监控，API 访问 |
| Enterprise | 定制 | 私有部署，SSO，专属模型调优 |

#### Milestone 2: 数据壁垒（6 个月）

**核心护城河**：竞品数据库

- 每次分析结果沉淀到结构化数据库
- 形成跨产品、跨行业的知识图谱
- 新用户分析已知竞品时，直接复用历史数据
- 网络效应：用户越多 → 数据越全 → 分析越准 → 用户越多

#### Milestone 3: API 经济（9 个月）

开放 API，让其他产品集成竞品分析能力：
```
POST /api/v1/analysis
{
  "target": "Notion",
  "dimensions": ["features", "pricing"],
  "format": "markdown"
}
```

典型集成场景：
- CRM 系统中自动拉取竞品情报
- 投资研报平台中自动生成竞品章节
- 企业内部知识库中嵌入实时竞品监控

#### Milestone 4: 垂直行业化（12 个月）

| 行业 | 定制点 |
|---|---|
| **SaaS 软件** | 功能矩阵、定价模型、API 生态 |
| **电商零售** | 价格监控、SKU 对比、用户评价情感 |
| **AI 产品** | 模型能力、API 定价、开源生态 |
| **硬件/IoT** | 供应链、专利布局、渠道策略 |
| **金融科技** | 合规要求、牌照、风控模型 |

### 4.3 收入模型预测

| 阶段 | 时间 | 用户数 | MRR |
|---|---|---|---|
| MVP 内测 | 月 1-3 | 50-100 | $0 |
| 公测 | 月 4-6 | 500-1000 | $5k-10k |
| 增长 | 月 7-12 | 2000-5000 | $30k-80k |
| 规模化 | 年 2 | 10000+ | $200k+ |

### 4.4 竞争壁垒

| 壁垒 | 实现方式 |
|---|---|
| **数据壁垒** | 用户分析结果沉淀为共享竞品知识库 |
| **模型壁垒** | 竞品分析专用 fine-tuned 模型（比通用 LLM 更准） |
| **生态壁垒** | 与 Slack/Notion/Jira 深度集成，嵌入工作流 |
| **品牌壁垒** | 成为"竞品分析"品类的代名词 |

---

## 5. 技术架构演进

### 5.1 当前架构

```
┌─────────┐     ┌──────────────┐     ┌──────────┐
│Streamlit│────▶│ Orchestrator │────▶│ FileStore│
│  UI     │     │  (In-Memory) │     │ (JSON)   │
└─────────┘     └──────┬───────┘     └──────────┘
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
      Collector    Analyst      Writer  Reviewer
      (同步顺序执行，单线程)
```

### 5.2 目标架构（Phase 4 完成后）

```
┌─────────────────────────────────────────────────────────┐
│                      API Gateway                         │
│              (Auth / Rate Limit / Billing)               │
└────────┬──────────┬──────────┬──────────┬───────────────┘
         ▼          ▼          ▼          ▼
    ┌────────┐┌────────┐┌────────┐┌────────────┐
    │Web UI  ││CLI     ││REST API││Slack Bot   │
    │(Next.js)││       ││        ││            │
    └───┬────┘└───┬────┘└───┬────┘└─────┬──────┘
        └─────────┴─────────┴───────────┘
                      │
         ┌────────────▼────────────┐
         │    Task Queue (Celery)  │
         │    + Redis Broker       │
         └────────────┬────────────┘
                      │
         ┌────────────▼────────────┐
         │  LangGraph Cloud /      │
         │  Self-Hosted DAG Engine │
         │  (并行执行，自动重试)     │
         └────────────┬────────────┘
                      │
    ┌─────────────────┼─────────────────┐
    ▼                 ▼                 ▼
┌────────┐    ┌─────────────┐    ┌──────────┐
│PostgreSQL│   │  Redis       │    │Vector DB │
│(业务数据) │   │(缓存/消息)    │    │(知识检索) │
└────────┘    └─────────────┘    └──────────┘
                      │
    ┌─────────────────┼─────────────────┐
    ▼                 ▼                 ▼
┌────────┐    ┌─────────────┐    ┌──────────┐
│LLM Pool │   │Search Pool  │    │Scraper   │
│(多模型)  │   │(Tavily+自建) │    │(Playwright)│
└────────┘    └─────────────┘    └──────────┘
```

### 5.3 关键技术选型建议

| 组件 | 当前 | 建议升级 | 原因 |
|---|---|---|---|
| 编排引擎 | LangGraph（未启用） | Temporal / Prefect | 生产级重试、超时、可观测性 |
| 消息队列 | 内存 MessageBus | Redis Pub/Sub + Celery | 持久化、解耦、水平扩展 |
| 数据库 | 无（JSON 文件） | PostgreSQL + Redis | 查询、索引、并发 |
| 向量存储 | 无 | pgvector / Milvus | 竞品知识检索、RAG |
| 前端 | Streamlit | Next.js + FastAPI | 性能、SEO、定制化 |
| LLM 网关 | 直连 | LiteLLM / Portkey | 多模型 fallback、负载均衡、成本追踪 |
| 爬虫 | httpx + regex | Playwright + Scrapy | JS 渲染、反爬对抗 |
| 监控 | loguru | OpenTelemetry + Grafana | 分布式追踪 |

### 5.4 数据模型演进

```
MVP (当前):
  CompetitorProfile ──→ FeatureMatrix ──→ MarketInsight ──→ Report

V2 (SaaS):
  User ──→ Project ──→ Analysis ──→ Report
                              │
                              ├── CompetitorProfile[]
                              ├── FeatureMatrix
                              ├── MarketInsight[]
                              └── ReviewResult

  Competitor (全局共享) ──→ 历史分析缓存
    ├── 基本信息
    ├── 功能快照历史
    ├── 定价变化时间线
    └── 舆情趋势

V3 (知识图谱):
  (Product)──[competes_with]──(Product)
     │                            │
     ├── [has_feature]──(Feature)
     ├── [uses_tech]──(Technology)
     ├── [funded_by]──(Investor)
     └── [targets]──(Market)
```

---

## 6. 立即可以做的 5 件事

按投入产出比排序：

| 排序 | 改进 | 耗时 | 收益 |
|---|---|---|---|
| 1 | 跑通 LangGraph 真实集成 + 并行执行 | 2h | 性能提升 3x，解锁生产部署 |
| 2 | Prompt 从代码迁移到独立 YAML | 1h | 非技术人员也能调 prompt |
| 3 | `_invoke_llm` 加重试机制 | 30min | 减少 90% 的 LLM 调用失败 |
| 4 | 输入校验（长度限制 + 特殊字符过滤） | 30min | 防止注入和崩溃 |
| 5 | 导出 PDF 功能 | 1h | 报告可分享，商业化必须 |

---

> **一句话总结**：MVP 的骨架是稳固的（架构设计、测试覆盖、溯源能力），但肌肉（LLM 可靠性、Prompt 调优、异常处理）和皮肤（UI、导出、集成）需要按路线图逐步补齐，才能从"能跑"进阶到"能用"再到"能卖"。
