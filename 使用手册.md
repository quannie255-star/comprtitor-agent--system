# 🔍 竞品分析 Agent 协作系统 — 使用手册

---

## 目录

1. [系统概述](#1-系统概述)
2. [环境准备](#2-环境准备)
3. [配置指南](#3-配置指南)
4. [命令行使用](#4-命令行使用)
5. [Streamlit 前端](#5-streamlit-前端)
6. [报告解读](#6-报告解读)
7. [溯源与调试](#7-溯源与调试)
8. [常见问题](#8-常见问题)

---

## 1. 系统概述

本系统由 4 个专职 AI Agent 组成，按 DAG 流水线自动完成竞品分析：

```
你输入 → Collector ──→ Analyst ──→ Writer ──→ Reviewer ──→ Markdown 报告
           搜索+抓取      SWOT+对比    报告生成      质检
```

| Agent | 做什么 | 多久完成 |
|---|---|---|
| **Collector** 采集员 | 搜索目标产品 → 抓取网页 → 提取结构化数据 | ~10s |
| **Analyst** 分析师 | SWOT 分析 + 功能对比 + 市场定位 + 趋势判断 | ~5s |
| **Writer** 撰写师 | 拼装报告章节 → 渲染 Markdown → 保存文件 | ~3s |
| **Reviewer** 质检员 | 事实核查 + 逻辑一致性 + 溯源验证 + 打分 | ~3s |

**核心能力**：
- 每条分析结论都带证据引用 `[source_id]`
- 质检不通过自动回退重跑（最多 3 轮）
- 全链路执行轨迹可追溯

---

## 2. 环境准备

### 2.1 前置要求

- Python >= 3.11
- pip

### 2.2 安装

```bash
cd ai-competitive-analysis

# 安装项目依赖
pip install -e ".[dev]"

# 如果上面的命令报错，手动安装核心依赖：
pip install streamlit loguru pydantic pyyaml httpx python-dotenv typing-extensions
```

### 2.3 配置 API Key

复制环境变量模板：

```bash
cp .env.example .env
```

编辑 `.env`：

```env
# LLM API Key（必须，支持 OpenAI / DeepSeek）
LLM_API_KEY=sk-your-key-here
LLM_API_BASE=

# Tavily 搜索 API Key（可选，不填使用 mock 数据）
TAVILY_API_KEY=tvly-your-key-here
```

**如果暂时没有 API Key**：系统会自动降级为 mock 模式，使用模拟数据演示完整流程。适合体验系统功能，但不能用于真实分析。

### 2.4 验证安装

```bash
# 运行全部测试（128 条），确认环境正常
pytest tests/ -v -q
```

最后一行应显示：`128 passed`

---

## 3. 配置指南

### 3.1 配置文件位置

| 文件 | 用途 |
|---|---|
| `.env` | API Key（不提交到 Git） |
| `config/settings.yaml` | LLM 参数、搜索配置、Agent 行为、存储路径 |
| `config/agents.yaml` | Agent 角色定义、Prompt 模板、工具列表 |

### 3.2 LLM 提供商配置

OpenAI：
```yaml
llm:
  provider: "openai"
  model: "gpt-4o"
  api_key: "${LLM_API_KEY}"
  temperature: 0.3
  max_tokens: 4096
```

DeepSeek（国内可用，性价比高）：
```yaml
llm:
  provider: "deepseek"
  model: "deepseek-chat"
  api_key: "${LLM_API_KEY}"
  api_base: "https://api.deepseek.com/v1"
  temperature: 0.3
  max_tokens: 4096
```

### 3.3 搜索配置

```yaml
search:
  provider: "tavily"              # 不填 API Key 则用 mock
  api_key: "${TAVILY_API_KEY}"
  max_results_per_query: 10
  timeout_seconds: 30
```

### 3.4 分析维度自定义

在 `config/settings.yaml` 中调整对比维度：

```yaml
agents:
  analyst:
    comparison_dimensions:
      - "核心功能"
      - "定价策略"
      - "用户体验"
      - "技术架构"
      - "市场定位"
      - "安全合规"       # 按需增减
```

### 3.5 质检严格度

```yaml
agents:
  reviewer:
    strict_mode: true            # 严格模式
    min_evidence_score: 0.7      # 证据置信度低于 0.7 会报警
    max_review_rounds: 3         # 最多回退 3 轮
```

---

## 4. 命令行使用

### 4.1 快速开始

```python
from core.orchestrator import Orchestrator

# 1. 配置
config = {
    "llm": {
        "provider": "openai",
        "model": "gpt-4o",
        "api_key": "sk-xxx",              # 替换为你的 Key
    },
    "search": {"api_key": ""},             # 空 Key → mock 搜索
    "agents": {
        "analyst": {"comparison_dimensions": ["功能", "定价", "体验"]},
        "reviewer": {"max_review_rounds": 3},
    },
    "orchestrator": {"recursion_limit": 25},
}

# 2. 创建编排器
orch = Orchestrator(config)

# 3. 运行分析
result = orch.run("Notion", use_langgraph=False)

# 4. 查看结果
print(result["report"])         # Markdown 报告
print(result["review_result"])  # 质检结果
print(orch.get_summary())       # 执行摘要
```

### 4.2 无 API Key 的 mock 模式

```python
# 不填 api_key，系统自动降级为 mock
config = {
    "llm": {"provider": "openai", "model": "gpt-4o", "api_key": ""},
    "search": {"api_key": ""},
}
orch = Orchestrator(config)
result = orch.run("Slack")
```

mock 模式下：
- 搜索返回模拟结果（example.com）
- LLM 返回占位输出
- 报告章节结构完整，但内容标注"待 LLM 分析"
- **适合验证流程、调试代码**

### 4.3 查看执行轨迹

```bash
# 报告保存在 outputs/
ls outputs/

# 执行轨迹保存在 traces/{trace_id}/
ls traces/

# 中间产物保存在 artifacts/{trace_id}/
ls artifacts/
```

---

## 5. Streamlit 前端

### 5.1 启动

```bash
python -m streamlit run src/api/routes.py
```

浏览器访问 `http://localhost:8501`

### 5.2 使用步骤

1. **左侧边栏** — 填入 LLM API Key 和 Tavily Key（可选）
2. **选择模型** — GPT-4o / DeepSeek / Claude
3. **勾选分析维度** — 默认：功能、定价、体验、市场定位
4. **主区域** — 输入竞品名称（如 `Notion`、`Figma`、`飞书`）
5. **点击"🚀 开始分析"** — 等待 10-30 秒
6. **查看报告** — Markdown 格式直接渲染展示
7. **展开"🔗 全链路溯源信息"** — 查看每个 Agent 的消息流向

### 5.3 页面布局说明

| 区域 | 内容 |
|---|---|
| 侧边栏 | API Key 配置、模型选择、分析维度多选 |
| 主区域上方 | 竞品名称输入 + 开始按钮 |
| 主区域中部 | 完整 Markdown 报告 |
| 主区域下方 | 执行摘要（Trace ID、各 Agent 步骤数、质检评分） |
| 可展开面板 | 消息日志时间线 |

---

## 6. 报告解读

### 6.1 报告结构

```
# 竞品分析报告
> 生成时间: 2025-06-01 14:30
> 追踪 ID: a1b2c3d4

## 一、执行摘要
3-5 句话概述分析范围、关键发现、核心结论

## 二、竞品概览
### 2.1 产品名称
| 属性 | 信息 |
核心功能列表、优势/劣势（每条带 [source_id]）

## 三、功能对比矩阵
| 维度 | 竞品A | 竞品B |
对比总结

## 四、市场洞察
SWOT 分析表格 + 差异化优势 + 趋势洞察（每条带 [source_id]）

## 五、战略建议
1-N 条优先级排序的行动建议

## 六、参考来源
1. [src_001] 来源标题 (URL) 置信度
```

### 6.2 证据引用格式

报告中的 `[src_001]` 标记表示该结论来自某个数据源，可在报告末尾"参考来源"章节找到对应 URL。

```
- 界面直观易用 [src_001]
...
## 六、参考来源
1. [src_001] Notion 官方产品页 (https://notion.so) （置信度: 95%）
```

### 6.3 质检评分解读

| 评分 | 含义 |
|---|---|
| 0.8 - 1.0 | 高质量报告，证据充分，可直接使用 |
| 0.6 - 0.8 | 报告可用，但部分结论证据不足 |
| 0.4 - 0.6 | 存在较多问题，建议人工复核 |
| 0.0 - 0.4 | 报告不可用，系统会自动驳回重跑 |

---

## 7. 溯源与调试

### 7.1 轨迹目录结构

```
traces/{trace_id}/
├── timeline.json         # 时间线摘要（所有步骤按时间排列）
├── messages.json         # Agent 间消息日志
└── agents/
    ├── collector.json    # 搜索/抓取/解析 每步详情
    ├── analyst.json      # 功能对比/SWOT/洞察 每步详情
    ├── writer.json       # 摘要生成/建议生成/报告保存 每步详情
    └── reviewer.json     # 编程检查/LLM审查/路由决策 每步详情

artifacts/{trace_id}/
├── 01_collector.json     # 采集原始结果
├── 02_analyst.json       # 分析中间结果
├── 04_writer.json        # 报告元信息
└── 05_reviewer.json      # 质检结论
```

### 7.2 如何排查问题

**报告内容不准确**：
```bash
# 查看 Collector 采集的原始数据
cat artifacts/{trace_id}/01_collector.json

# 查看 Analyst 的分析过程
cat traces/{trace_id}/agents/analyst.json
```

**质检总是不过**：
```bash
# 查看 Reviewer 的 issue 列表
cat traces/{trace_id}/agents/reviewer.json | grep -A5 "issues"
```

**想知道哪个步骤慢**：
```bash
# 查看时间线
cat traces/{trace_id}/timeline.json | python -m json.tool
```

### 7.3 Agent 间消息追踪

```python
# Python 中查看消息日志
from core.orchestrator import Orchestrator

orch = Orchestrator(config)
orch.run("Notion")

for msg in orch.bus.get_message_log():
    print(f"[{msg.timestamp.strftime('%H:%M:%S')}] {msg.sender} → {msg.receiver}: {msg.msg_type.value}")
```

输出示例：
```
[14:30:01] collector → analyst: data_output
[14:30:05] analyst → writer: data_output
[14:30:08] writer → reviewer: data_output
[14:30:11] reviewer → BROADCAST: task_complete
```

---

## 8. 常见问题

### Q1: "LLM API Key 未配置，Agent 将使用占位输出"

**原因**：`.env` 文件未配置或 `api_key` 为空。

**解决**：在 `.env` 中填入 `LLM_API_KEY=sk-xxx`，或在代码中传入 `api_key`。

**影响**：不配置也能跑通全流程，但报告内容是占位的（标注"待 LLM 分析"），不能用于真实分析。

### Q2: "Search API key 未配置，返回模拟结果"

**原因**：未配置 Tavily Key。

**解决**：在 `.env` 中填入 `TAVILY_API_KEY=tvly-xxx`，或在代码中传入。

**影响**：搜索返回 mock 数据（example.com），采集结果不真实。

### Q3: 质检一直不通过，不断回退

**原因**：上游数据质量太差（mock 模式常见）。

**解决**：
- 配置真实 API Key
- 降低质检严格度：`min_evidence_score: 0.5`
- 减小最大回退轮次：`max_review_rounds: 1`

### Q4: Streamlit 启动失败

```bash
# 试试 Python 模块方式启动
python -m streamlit run src/api/routes.py

# 如果还不行，检查安装
python -c "import streamlit; print(streamlit.__version__)"
```

### Q5: 分析某个产品时崩溃

**排查步骤**：
1. 查看 `traces/{trace_id}/` 下对应 Agent 的日志
2. 确认 LLM 返回了有效 JSON（有时 LLM 会输出版权声明等非 JSON 内容）
3. 如果是网络问题（抓取超时），增加 `timeout_seconds`

### Q6: 如何添加新的分析维度

编辑 `config/settings.yaml`：

```yaml
agents:
  analyst:
    comparison_dimensions:
      - "核心功能"
      - "定价策略"
      - "用户体验"
      - "技术架构"
      - "市场定位"
      - "你的新维度"    # ← 添加这里
```

同时在 Streamlit 侧边栏的 `all_dims` 中添加同名项。

### Q7: 支持分析中文产品吗？

支持。输入中文产品名（如 `飞书`、`钉钉`、`企业微信`），系统会用中文搜索和分析。推荐使用 DeepSeek 作为 LLM 后端，中文效果更好。
