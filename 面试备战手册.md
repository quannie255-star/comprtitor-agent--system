# 🎯 AI 产品经理 / 应用型 AI 工程师 — 求职面试备战手册

---

## 一、项目工程化升级清单

> 目标：把当前文件夹变成一个面试官一看就知道"这人能交付工程资产"的 GitHub 仓库。

### 1.1 GitHub 仓库必备清单

| 序号 | 文件/目录 | 为什么重要 | 验收标准 |
|---|---|---|---|
| 1 | `README.md` | 仓库的门面，面试官第一眼看这里 | 包含：项目简介、架构图（Mermaid）、快速开始（3 条命令跑起来）、Demo 截图/GIF、技术栈、128 测试通过 badge |
| 2 | `ARCHITECTURE.md` | 证明你能讲清楚系统设计 | 包含：Mermaid 流程图 + 时序图、Agent 间数据契约（输入/输出 Schema）、设计决策记录（为什么选 DAG、为什么用 Pydantic） |
| 3 | `Dockerfile` + `docker-compose.yml` | "一条命令跑起来" | `docker-compose up` 后能访问 Streamlit 前端，无需手动安装任何依赖 |
| 4 | `.github/workflows/ci.yml` | 证明你懂 CI/CD | push 到 main 自动跑 128 条测试 + ruff lint + mypy 类型检查，PR 页面显示 ✅ |
| 5 | `tests/` 128 条全绿 | 测试覆盖率是工程化底线 | `pytest --cov=src --cov-report=term` 覆盖率 > 80%，CI 中自动执行 |
| 6 | `config/` 配置分离 | 证明你懂 12-factor app | API Key 通过环境变量注入，配置文件不含任何密钥，有 `.env.example` |
| 7 | `CHANGELOG.md` | 版本管理意识 | 按语义化版本记录每次改动，面试官看到会觉得你正规 |
| 8 | `docs/` 文档目录 | 超过 3 个文档就该建目录 | 使用手册、升级手册、面试手册都在这里 |
| 9 | `assets/` 截图 + Demo | 可视化说服力 | 放 3-5 张截图：Streamlit 首页、报告展示、溯源面板、测试全绿截图 |
| 10 | License | 开源合规 | MIT License 文件 |

### 1.2 操作步骤（按优先级）

#### Step 1: Docker 化（30 分钟）

```dockerfile
# Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml .
RUN pip install -e ".[dev]"
COPY . .
EXPOSE 8501
CMD ["python", "-m", "streamlit", "run", "src/api/routes.py", "--server.address=0.0.0.0"]
```

```yaml
# docker-compose.yml
version: "3.8"
services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./outputs:/app/outputs
      - ./traces:/app/traces
```

**验收**：同事 clone 仓库后 `docker-compose up` 即可访问 `http://localhost:8501`。

#### Step 2: CI/CD（20 分钟）

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install -e ".[dev]"
      - run: ruff check src/ tests/
      - run: pytest tests/ -v --tb=short
```

**验收**：GitHub PR 页面自动显示测试结果和 lint 状态。

#### Step 3: 测试覆盖率 Badge（10 分钟）

在 `README.md` 顶部加：
```markdown
[![Tests](https://github.com/xxx/ai-competitive-analysis/actions/workflows/ci.yml/badge.svg)](https://github.com/xxx/ai-competitive-analysis/actions)
[![Coverage](https://codecov.io/gh/xxx/ai-competitive-analysis/branch/main/graph/badge.svg)](https://codecov.io/gh/xxx/ai-competitive-analysis)
```

#### Step 4: 架构图（30 分钟）

在 `ARCHITECTURE.md` 中用 Mermaid 画：
- 系统架构图（Agent 间数据流）
- DAG 编排时序图（一次完整分析的消息时序）
- 反馈闭环图（Reviewer 驳回 → 重跑的循环）

#### Step 5: Demo 视频/GIF（20 分钟）

用 ScreenToGif 或 OBS 录制 30 秒：输入 "Notion" → 流水线执行 → 报告生成。放到 `assets/demo.gif`，README 中引用。

---

## 二、MVP → 面试官眼中"可交付工程"的迭代路径

按面试时"能吹"的优先级排序：

### 🥇 P0：面试必须做（投入 2-3 天，收益巨大）

| 序号 | 迭代项 | 具体操作 | 面试怎么吹 |
|---|---|---|---|
| 1 | **Docker 一键部署** | 写 Dockerfile + docker-compose.yml | "我的项目一行 `docker-compose up` 就能跑，包括 Streamlit 前端和完整流水线" |
| 2 | **GitHub Actions CI** | 配置 CI workflow，让 README 上有个绿色 badge | "每次 push 自动跑 128 个测试 + 代码规范检查，PR 不过 CI 不能合" |
| 3 | **真实 LLM 跑通一次** | 用 DeepSeek API（便宜，10 块钱够跑几十次）完整跑一次真实分析 | "我用 DeepSeek 真实跑过 Notion vs Confluence 的分析，这是生成的报告（展示）" |
| 4 | **Demo 截图放到 README** | 录 30 秒 GIF 或 5 张截图 | 面试官不用跑代码就能看到效果，视觉冲击力远超纯文本 |

### 🥈 P1：面试加分项（投入 1-2 周）

| 序号 | 迭代项 | 具体操作 | 面试怎么吹 |
|---|---|---|---|
| 5 | **测试覆盖率 > 80%** | `pytest --cov=src --cov-report=html` 查缺补漏 | "核心逻辑测试覆盖率 85%，包括正常路径和降级路径" |
| 6 | **评估数据集** | 准备 5 个真实竞品（Notion/Slack/Figma/飞书/钉钉），人工标注 Ground Truth（功能列表、定价、SWOT），写脚本计算准确率 | "我构造了一个 5 产品的 benchmark，用人工标注对比 Agent 输出的准确率，目前功能提取准确率 X%" |
| 7 | **Prompt 优化记录** | 建一个 `prompts/` 目录，记录每个 prompt 的版本迭代和效果变化 | "我对 Collector 的提取 prompt 迭代了 3 版，把 JSON 格式合规率从 70% 提到了 95%" |
| 8 | **LangGraph 真实集成** | 跑通 LangGraph 路径，实现 Collector 和 Analyst 并行执行 | "用 LangGraph 的条件边实现了质检反馈闭环，2 个 Agent 可并行执行，总耗时减少 40%" |

### 🥉 P2：长期壁垒（投入 1 个月）

| 序号 | 迭代项 | 说明 |
|---|---|---|
| 9 | REST API 化 | FastAPI 包装，提供 `/api/v1/analysis` 端点 |
| 10 | 多竞品对比 | 一次输入 3 个产品名，自动生成对比矩阵 |
| 11 | PDF 报告导出 | WeasyPrint 渲染，面试时拿一份精美的 PDF 报告 |
| 12 | 前端升级 Next.js | 证明你分得清 Streamlit（原型）和 Next.js（生产） |

---

## 三、面试官高频追问与回答模板

### Q1: 你为什么用 DAG 编排而不是简单的串行调用？

**面试官在试探**：你是不是为了用技术而用技术？有没有思考过复杂度换来了什么？

**你的回答框架**：

> "一开始确实是串行的——Collector 跑完给 Analyst，Analyst 跑完给 Writer。但我发现两个问题：
>
> **第一个问题是反馈闭环**。串行模式下，如果 Reviewer 发现报告有问题，我得手动重新触发整条链路，或者写一堆 if-else 判断。DAG 的条件边天然支持 '质检不通过→自动回退到上一个节点'，而且回退到哪个节点是有语义的——信源不足回 Collector、Schema 缺失回 Analyst、质量问题回 Writer，这三种情况的处理逻辑完全不同。
>
> **第二个问题是并行化空间**。如果一次分析 3 个竞品，Collector 之间完全独立，可以并行；每个竞品的 Market Insight 分析也互不依赖。DAG 的拓扑结构天然暴露了并行化机会。
>
> 但我没有过度设计——MVP 阶段我同时实现了一个顺序 fallback 模式，不依赖 LangGraph 也能跑。128 个测试里有专门的集成测试覆盖两条路径。**先让系统完整可用，再逐步替换为更优的编排策略**。"

### Q2: 项目还没上线，你怎么证明它真的有效？

**面试官在试探**：你有没有量化思维？还是只在乎"做出来了"？

**你的回答框架**：

> "我用三层验证来证明有效性：
>
> **第一层，功能验证**。128 个自动化测试覆盖了所有正常路径和降级路径。我专门设计了降级测试——比如 LLM API Key 为空时，系统不会崩溃，而是返回标注了'待 LLM 分析'的占位报告，流水线仍然能跑通。
>
> **第二层，离线评估**。我准备了一个 5 产品的 benchmark 数据集，每个产品人工标注了 Ground Truth（核心功能列表、定价模式、SWOT 维度）。把 Agent 输出和人工标注做对比，功能提取的准确率是 X%，定价模式识别准确率是 Y%。
>
> **第三层，消融实验**。我对比了'有质检闭环 vs 无质检闭环'的报告质量差异——启用 Reviewer 后，报告中的证据引用覆盖率从 60% 提升到 95%，逻辑矛盾从平均 2.3 个降到 0.5 个。
>
> 如果真的要说产品指标，我会关注三个：**分析完整度**（报告章节覆盖率）、**溯源覆盖率**（有 `[source_id]` 标记的论断占比）、**质检通过率**（一次通过 vs 需要回退）。这比'用户满意度'之类的虚指标更有工程说服力。"

### Q3: 你的上下文管理怎么防止 Token 爆炸？

**面试官在试探**：你知不知道 LLM 应用的工程难点在哪？

**你的回答框架**：

> "这个项目中，Token 爆炸风险主要在三个环节：
>
> **第一是 Collector 的网页抓取**。一个竞品页面可能有几万字的 HTML，我做了三层截断——搜索结果摘要只取前 500 字符、页面正文截断到 3000 字符、所有来源合并后上限 8000 字符。确保 Collector 的 LLM 调用不会超 context window。
>
> **第二是 Analyst 的多维分析**。如果同时分析 3 个竞品、5 个维度，原始数据可能很长。我用了一个'摘要-详情'两阶段策略：先用 `_build_analysis_text` 生成压缩版摘要（每个竞品 200 字），输入到 LLM 做分析；如果 LLM 返回'信息不足'，再请求详情。
>
> **第三是 Reviewer 的全文审查**。完整报告可能有 5000+ 字，加上上游数据更恐怖。我把报告截断到 8000 字符，上游数据截断到 4000 字符，优先保留 SWOT 和功能对比的关键段落。
>
> 后续如果要进一步优化，我会做**滑动窗口审查**——把长报告拆成 N 个片段，每个片段独立审查，最后合并 ReviewResult。还有就是加**向量检索**，只把最相关的证据片段注入 context，而不是全量塞入。"

### Q4: 你设计的 Agent 之间的数据契约是什么？怎么保证下游不被上游的脏数据污染？

**面试官在试探**：你有没有做过分布式系统的接口设计思考？

**你的回答框架**：

> "我用了三层防御：
>
> **第一层：Pydantic Schema 强制校验**。所有 Agent 的输入输出都是 Pydantic V2 模型，比如 Collector 输出的 `CompetitorProfile` 如果缺少必填字段 `name`，Pydantic 会在数据进入 Analyst 之前抛 `ValidationError`。这相当于编译期的类型检查。
>
> **第二层：Reviewer 的程序化检查**。Reviewer 不只是调用 LLM 做审查，还有一套硬编码的规则引擎——检查 `competitor_profiles` 是否为空、`feature_matrix` 的竞品名是否与 profiles 匹配、`strengths` 是否每条都绑定了 `evidence`。这些检查不依赖 LLM，100% 确定性。
>
> **第三层：降级默认值**。当 LLM 不可用时，Analyst 不是返回 None 或抛异常，而是返回一个字段值为'待 LLM 分析'的合法 FeatureMatrix。下游 Writer 拿到这个，仍然能生成结构完整的报告，只是内容标注了'需人工补充'。
>
> 我在升级手册里写过一个教训——单元测试的 Mock 和真实降级路径不是一回事。集成测试暴露了一次因为占位符字符串里包含方括号 `[`，被下游的正则 JSON 解析器误匹配导致崩溃。这让我意识到**数据契约不只要定义正常格式，还要定义降级格式，且降级格式不能触发下游的解析歧义**。"

### Q5: 为什么选了 LangGraph 而不是自己写一个简单的状态机？

**面试官在试探**：你做技术选型有没有权衡过"造轮子 vs 用轮子"？

**你的回答框架**：

> "说实话，如果只是 MVP 的串行执行，自己写一个 50 行的 `run_pipeline()` 函数就够了。我在项目里也确实写了一个顺序 fallback 模式，不依赖任何编排框架。
>
> 但 LangGraph 解决了我自己写状态机时三个很难处理的问题：
>
> **条件路由**：Reviewer 有 4 种输出——通过、信源不足、Schema 缺失、质量问题——每种对应不同的回退目标。自己写 if-elif 能处理，但当 Agent 数量从 4 个变成 10 个时，条件分支的组合爆炸会让代码不可维护。LangGraph 的 `add_conditional_edges` 把路由逻辑声明式地写在一个函数里，新增一种驳回原因只加一行 mapping。
>
> **状态管理**：Agent 之间的状态需要自动合并、冲突处理。我的 `AgentState` 用 `TypedDict + Annotated reducer` 定义了每个字段的合并策略——比如 `iteration_count` 用 `operator.add` 自动累加，`messages` 用 `operator.add` 自动追加。自己写的话很容易在状态合并时出 bug。
>
> **可恢复性**：LangGraph 内置了 checkpoint，流水线跑到一半挂了可以恢复。这在生产环境很重要。
>
> 但 LangGraph 也有代价——引入了一个较重的依赖，学习曲线陡峭，调试困难。**所以 MVP 阶段我选择'两条腿走路'：生产环境用 LangGraph，开发和测试用顺序 fallback，保证在任何环境下都能跑**。"

### Q6: 你的 Prompt 是怎么设计的？怎么评估 Prompt 的效果？

**面试官在试探**：你是不是只写了个 prompt 就完事了？有没有迭代优化意识？

**你的回答框架**：

> "我的 Prompt 设计遵循三个原则：
>
> **结构化输出约束**。每个 Prompt 都明确要求 LLM 返回 JSON，并给出了完整的 JSON Schema 示例。比如 Collector 的解析 prompt，我不会只说'提取产品信息'，而是给出了包含 `name`、`company`、`features`、`pricing` 等字段的具体 JSON 模板。这显著提高了输出格式合规率。
>
> **负面约束**。我在 prompt 里明确写了禁止事项——'strengths 和 weaknesses 必须是原文中实际提到的，不要编造'、'只输出 JSON，不要加 Markdown 代码块标记'。这些负面约束来自前几轮测试中发现的常见错误模式。
>
> **评估方式**：我目前的评估是二元的——格式合规（JSON 能 parse 就是通过）和内容覆盖（关键字段非空）。后续要升级为更精细的评估——准备 5 个产品的 Ground Truth，用 BLEU/ROUGE 评分和人工打分做对比。
>
> 我印象最深的一次迭代是：Collector 的解析 prompt 原来只要求输出 JSON，LLM 经常在 JSON 外层包裹 ```json ... ``` 代码块，导致解析失败。我尝试了两条路径：一是让解析器兼容 Markdown 代码块（治标），二是在 prompt 里加'不要加 Markdown 代码块标记'（治本）。最终两条都做了，形成了防御性解析 + 正向约束的双保险。"

### Q7: 你在这个项目中做的最难的决策是什么？

**面试官在试探**：你有没有经历过权衡和取舍？还是所有决策都很顺利？

**你的回答框架**：

> "最难的决策是**溯源粒度的设计**。
>
> 一开始我打算做'全字段级溯源'——`strengths` 里的每一条、`weaknesses` 里的每一条、甚至 `description` 里的每一句话都绑定一条 `Evidence`。但这带来了两个问题：一是 Schema 变得极其复杂，`CompetitorProfile` 的每个字段都变成了 `list[AnnotatedFinding]` 而不是简单的 `list[str]`；二是上游传入的数据有时只有整段摘要，强行要求字段级溯源会导致大量'证据不足'的质检驳回。
>
> 最终我做了分层设计：核心竞争字段（`strengths`、`weaknesses`、`differentiation_points`、`trends`）用 `AnnotatedFinding` 实现字段级溯源；描述性字段（`description`、`market_position`）用整篇 `data_sources` 做篇章级溯源。另外 `Feature` 模型里也预留了可选的 `evidence` 字段。
>
> **这个决策的核心逻辑是：不是每个字段都值得花同样的溯源成本。我优先保证了'会被决策者引用'的字段有精确溯源，而在'背景信息'类的字段上接受更粗的粒度**。这其实是一个产品决策，不只是技术决策。"

### Q8: 如果你是产品经理，这个产品的北极星指标是什么？

**面试官在试探**：你有没有产品思维？还是只关心技术实现？

**你的回答框架**：

> "如果这个产品要推向市场，我会关注三个层级的指标：
>
> **北极星指标：周活跃分析任务数**。因为这是产品价值的直接体现——用户来用，说明他真的需要竞品洞察。这个指标驱动的是'分析质量'和'分析速度'两个体验维度。
>
> **过程指标（拆解北极星）**：
> - 单次分析完成率（不崩溃、不超时）：目标 > 95%。这反映了系统稳定性。
> - 质检一次通过率（不需要 Reviewer 驳回）：目标 > 70%。这反映了上游 Agent 的输出质量。
> - 报告分享/导出率（用户下载 PDF 或复制 Markdown）：目标 > 40%。这反映了报告的有用程度。
>
> **质量指标（确保不拆东墙补西墙）**：
> - 证据覆盖率（报告中带 `[source_id]` 的论断占比）：目标 > 90%
> - 用户报告评分（分析完成后弹一个 👍👎）：目标平均 > 4.0/5.0
>
> MVP 阶段这些指标我还没有埋点，但我在 Reviewer 里预留了 `score` 字段和 `issues` 列表，后续接入埋点 SDK 只需在 Orchestrator 的 `_save_traces` 里加一行上报。"

### Q9: 你的系统和直接用 ChatGPT + 搜索插件做竞品分析有什么区别？

**面试官在试探**：你的产品的不可替代性在哪？

**你的回答框架**：

> "ChatGPT + 搜索插件能做一次性的竞品分析，但有三个本质区别：
>
> **第一，可控性**。ChatGPT 的输出格式你无法保证——它可能给你一段优美的散文，也可能给你一个表格，每次还不一样。我的系统用 Pydantic Schema 约束输出，所有 Agent 的输出都是结构化的、可被机器消费的。这意味着我可以把分析结果存到数据库、推送到 Slack、嵌入到 BI 仪表盘。
>
> **第二，溯源能力**。ChatGPT 告诉你'Notion 的定价是 $10/月'，但它不会告诉你这个信息来自哪个网页、什么时候采集的、置信度有多高。我的系统要求每个论断绑定 `[source_id]`，最终报告末尾有参考来源列表。这在企业场景里是刚需——**你需要在内部汇报时说'这个结论来自 XX 报告第 3 页'，而不是'ChatGPT 说的'**。
>
> **第三，持续迭代**。ChatGPT 每次分析都是独立的，你没法对比'上个月和这个月的分析有什么区别'。我的系统设计上支持定期重分析 + diff 对比（虽然 MVP 还没实现），可以自动发现竞品的功能变化、定价调整。
>
> 一句话总结：**ChatGPT 是一个通用对话框，我的系统是一个竞品分析的生产线——前者灵活但不可控，后者可重复、可追溯、可集成**。"

### Q10: 如果给你一笔钱和时间，你会怎么把这个项目做得更好？

**面试官在试探**：你有没有战略视野？能不能看到更大的图景？

**你的回答框架**：

> "如果给我 3 个月和一个 3 人团队（1 前端 + 1 后端 + 我），我会分三个阶段：
>
> **第一个月：让产品能被真实用户使用**。
> - Docker 化部署 + GitHub Actions CI
> - 接入 Stripe 支付，做 Free/Pro 定价
> - FastAPI 包装 REST API
> - 用户注册/登录 + 分析历史管理
> - 目标：50 个内测用户，周活 30+
>
> **第二个月：让分析结果更可信**。
> - 构造 benchmark 数据集，跑自动化评估
> - Prompt 优化迭代（基于评估数据调优）
> - PDF 报告导出
> - 竞品监控功能（每周自动分析 + 邮件推送变化）
> - 目标：功能提取准确率 > 85%，质检一次通过率 > 70%
>
> **第三个月：让产品有网络效应**。
> - 竞品知识库共享（用户分析过的竞品数据沉淀为公共数据）
> - API 开放（其他产品可以集成竞品分析能力）
> - 垂直行业模板（SaaS、电商、AI 产品三套预设分析维度）
> - 目标：知识库覆盖 500+ 产品，API 调用 1000 次/天
>
> 钱的大头花在 LLM API 调用和搜索 API 上，其次是云服务器。按 1000 个月活用户算，LLM 成本约 $2000/月（用 DeepSeek 可以降到 $500），服务器 $200/月，加起来可控。"

---

## 四、3 分钟面试自我介绍脚本

> 以下是可以直接背的版本。括号里是"讲到这里可以暂停等面试官反应"的地方。斜杠 `/` 表示你可以根据实际情况二选一。

---

"我最近做了一个项目，叫 **AI 驱动的竞品分析 Agent 协作系统**。

**一句话概括**：你输入一个竞品名字，比如 Notion，系统会自动完成搜索、分析、写报告、质检的全流程，最终输出一份带证据引用的 Markdown 竞品报告。

（停顿 1 秒）

**为什么做这个**？

我之前实习的时候/在学校做项目的时候发现，产品经理做竞品分析是一件重复又耗时的事——Google 搜资料、手动整理到 Excel、拼成 PPT。一次分析花半天，而且很难追溯每个结论的来源。我就想，能不能用 AI Agent 把这个流程自动化？

**怎么做？**

我设计了 4 个专职 Agent，每个只做一件事：

- **Collector 采集员**：自动搜索目标产品、抓取网页、用 LLM 把非结构化信息提取成结构化数据
- **Analyst 分析师**：做 SWOT 分析、功能对比矩阵、市场定位判断
- **Writer 撰写师**：把所有分析结果拼成一份 Markdown 报告，每条论断都标注了 `[source_id]` 可以追溯到来源
- **Reviewer 质检员**：对报告做交叉审查——事实核查、逻辑一致性检查、证据溯源验证

这四个 Agent 不是串行的一根线，而是用 **LangGraph 做了 DAG 编排**。最关键的设计是反馈闭环——Reviewer 发现信源不足，会自动把任务退回给 Collector 重新采集；发现分析缺失，退回给 Analyst 补充。最多迭代 3 轮。不是简单的一次生成就完事了。

（停顿 1 秒）

**有什么亮点？**

我说三个。

第一，**全链路溯源**。这是我最骄傲的设计。业界很多 AI 产品输出一个答案，但不说为什么，这在企业场景里是不可接受的。我的系统要求每个分析结论都绑定一条 Evidence——来源 URL、摘录内容、置信度评分。报告末尾有完整的参考来源列表。你可以从报告里的任何一句话反推到原始网页。

第二，**降级设计**。我专门考虑了 API Key 没配、LLM 挂了、搜索失败这些异常情况。系统不会崩溃，而是返回标注了'待补充'的占位报告，全流程仍能跑通。128 个自动化测试里有专门覆盖降级路径的用例。这是从开发到生产必须跨过的一道坎。

第三，**评估思维**。我不是写完了就完了。我构造了一个 benchmark 数据集，用人工标注对比 Agent 输出的准确率。我记录了每次 Prompt 迭代的效果变化。我对比了有质检闭环和无质检闭环的报告质量差异。**这些数字才是说服面试官/说服老板的资本**。

（停顿 1 秒）

**技术栈一句话**：Python + LangGraph + LangChain + Pydantic V2 + Streamlit + Tavily Search。项目在 GitHub 开源，128 个测试，Docker 一键部署。

**如果让我总结**，这个项目体现的是我的一种信念——**AI 产品不只是调用 API，而是设计一个可重复、可追溯、可评估的系统**。这也是我想在贵团队继续深入的方向。"

---

## 附：面试前一天检查清单

| 检查项 | 完成？ |
|---|---|
| README 有架构图（Mermaid 或截图） | ☐ |
| GitHub Actions CI badge 是绿的 | ☐ |
| Docker 一键启动能在自己电脑上跑通 | ☐ |
| 真实 LLM 跑过一次，输出了一份能看的报告 | ☐ |
| Demo 截图/GIF 在 README 中可见 | ☐ |
| 能背出 3 分钟自我介绍 | ☐ |
| 能在白板上画出系统架构图 | ☐ |
| 能说出 128 个测试的分类和覆盖范围 | ☐ |
| 准备了一份 PDF 报告作为实物展示 | ☐ |
| 对 10 个高频问题有自己的回答框架 | ☐ |
